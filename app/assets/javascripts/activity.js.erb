var getFun = (function(){
  'use strict';
  function cityFun(){
    var publisher = '<%= ENV['PUBLISHER_CODE'] %>';
    var city = $('input').val();
    var state = $('select').val;
    var location = city+','+state;
    $.ajax({
      //searches type-restaurant
      //where-pgh,pa, results per page-5, 
      //sort by distance, publisher-id, format-json
      url: 'https://api.citygridmedia.com/content/places/v2/search/where?type=restaurant&where=Pittsburgh,PA&rpp=5&sort=dist&publisher='+publisher+'&format=json',
      method: 'GET',
      dataType: 'JSON',
      success: function(data){
        
      }
    })
  }
});